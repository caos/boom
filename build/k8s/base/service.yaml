apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: boom
    app.kubernetes.io/part-of: orbos
    app.kubernetes.io/component: boom
    boom.caos.ch/application: boom
  name: boom-metrics
  namespace: caos-system
spec:
  ports:
    - name: metrics
      protocol: TCP
      port: 2112
      targetPort: 2112
    - name: debug
      protocol: TCP
      port: 5000
      targetPort: 5000
  selector:
    app.kubernetes.io/instance: boom
    app.kubernetes.io/part-of: boom
    app.kubernetes.io/component: boom
    boom.caos.ch/application: boom