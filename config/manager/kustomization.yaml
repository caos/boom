resources:
- manager.yaml

secretGenerator:
- files:
  - secret/id_rsa-toolsop-tools-read
  name: tools-secret

generatorOptions:
  disableNameSuffixHash: true
  
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
images:
- name: controller
  newName: controller
  newTag: latest
