apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: caos-system

configMapGenerator:
- name: logging-dashboard-rev3
  files:
  - logging-dashboard-rev3

generatorOptions:
 disableNameSuffixHash: true
